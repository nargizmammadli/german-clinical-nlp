---
phase: 03-deployment-documentation
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - README.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-04]

must_haves:
  truths:
    - "Portfolio reviewer can follow README Quick Start without reading anything else and reach a working curl test"
    - "README contains curl and Python httpx examples with full JSON response per D-06"
    - "README explains why decisions were made (local LLM, German clinical text, plugin architecture) per D-07"
    - "docs/ARCHITECTURE.md contains a component diagram showing the full request flow (main.py → pipeline → extractors → validators) per D-09"
    - "README links to docs/ARCHITECTURE.md for deeper architecture content per D-08"
  artifacts:
    - path: "README.md"
      provides: "Full portfolio README replacing single-line stub"
      contains: "## Quick Start"
    - path: "docs/ARCHITECTURE.md"
      provides: "Architecture doc with request-flow component diagram added"
      contains: "POST /extract"
  key_links:
    - from: "README.md"
      to: "docs/ARCHITECTURE.md"
      via: "Markdown link in Architecture section"
      pattern: "docs/ARCHITECTURE\\.md"
    - from: "README.md Quick Start"
      to: "docker-compose.yml"
      via: "docker-compose up command reference"
      pattern: "docker-compose up"
---

<objective>
Write the full portfolio README.md (replacing the current single-line stub) and supplement docs/ARCHITECTURE.md with the missing component diagram section (D-09 section 1).

Purpose: Enables a portfolio reviewer to understand, set up, and use the extraction API. The README is the primary entry point and must work as a standalone document. The architecture doc provides deeper technical content for reviewers who want to understand the design.

Output:
- README.md: 8-section portfolio document per D-05, D-06, D-07 (Quick Start first, API Reference with curl + Python httpx, example output inline snippet, explanatory Why This Project, Architecture link)
- docs/ARCHITECTURE.md: Component diagram section inserted before the Plugin Pattern section showing POST /extract → extract.py → asyncio.gather → extractors → EntityResponse → JSON flow
</objective>

<execution_context>
@/c/Users/nargi/.claude/gsd-core/workflows/execute-plan.md
@/c/Users/nargi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deployment-documentation/03-CONTEXT.md
@.planning/phases/03-deployment-documentation/03-RESEARCH.md
@.planning/phases/03-deployment-documentation/03-PATTERNS.md
@src/schemas/entities.py
@src/api/health.py
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write full portfolio README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md — read current content (currently a single-line stub: "# german-clinical-nlp"); full rewrite required
    - .planning/phases/03-deployment-documentation/03-CONTEXT.md — D-05 (Quick Start first), D-06 (curl + Python httpx with full JSON), D-07 (explanatory tone), D-08 (link to docs/architecture.md)
    - .planning/phases/03-deployment-documentation/03-RESEARCH.md — Pattern 4 (README section order), Python httpx code example, curl example (must include -H "Content-Type: application/json"), Pitfall 5 (missing Content-Type causes 422)
    - .planning/phases/03-deployment-documentation/03-PATTERNS.md — README section: documentation style (H2/H3 headers, fenced code blocks with language tags), section order from D-05/D-07
    - src/schemas/entities.py — EntityResponse fields: temporal_entities, clinical_entities, errors, low_confidence; TemporalEntity and ClinicalEntity fields for accurate JSON examples
    - src/api/health.py — /health response structure (status, model_loaded, timestamp) for API Reference section
    - docker-compose.yml — verify exact commands produced in Plan 01 are referenced correctly
  </read_first>
  <action>
    Write README.md with these 8 sections in order (per D-05, D-07):

    1. **Header:** Project title "German Clinical NLP Pipeline" + one-sentence description ("Production-ready information extraction pipeline for German clinical text — demonstrates local LLM deployment, structured output validation, and hallucination detection with FastAPI and llama-cpp-python.") + tech badge row (Python 3.11, FastAPI, llama-cpp-python, Docker, License MIT)

    2. **## Quick Start** (per D-05: recruiter can stop here):
       - Prerequisites: Docker 20.10+, docker-compose 2.x, 8GB RAM, 6GB free disk
       - Step 1: Clone and prepare — git clone, cd, cp .env.example .env
       - Step 2: Download model (~4.5GB) — mkdir -p models, wget command with TheBloke URL for mistral-7b-instruct-v0.2.Q4_K_M.gguf; note official mistral-community/Mistral-7B-Instruct-v0.2-GGUF as alternative source
       - Step 3: Start the API — docker-compose up; note "First run takes 30-60s for model to load"
       - Step 4: Test extraction — curl -X POST http://localhost:8000/extract -H "Content-Type: application/json" with a German clinical sentence containing dates, diagnosis, and medication (e.g., "Patient wurde am 15.03.2024 mit Diabetes mellitus Typ 2 aufgenommen. Verweildauer: 5 Tage. Metformin 1000mg täglich.")
       - Include expected response JSON showing temporal_entities, clinical_entities, errors, low_confidence arrays with actual field names from EntityResponse

    3. **## API Reference** — document all three endpoints with curl + Python httpx examples:
       - GET /health — curl + expected 200 response {"status": "healthy", "model_loaded": true, ...}
       - GET /models — curl + expected response with model name and quantization
       - POST /extract — curl example (must include -H "Content-Type: application/json") + Python httpx example (per D-06: complete request with German sentence and full formatted JSON response showing entity types, confidence scores, source spans)
       - Python httpx example must import httpx, create Client, post to /extract, print temporal_entities count and clinical_entities count, loop over entities printing type, text, confidence

    4. **## Example Extraction Output** — inline truncated JSON from temporal_extraction.json pattern:
       - Show input text (oncology timeline with admission/discharge dates in German)
       - Show response JSON with 2-3 extracted TemporalEntity items (type "Date" and "LOS"), confidence scores, source spans with start/end/text, source_span_validated: true
       - Add "See [data/examples/](data/examples/) for full extraction samples covering diagnoses, medications, and mixed entities."

    5. **## Why This Project** (per D-07: explanatory tone explaining decisions):
       - "Local LLM, not cloud API" — explain: no data leaves the machine, no API key, no latency, no cost, demonstrates production-ready self-hosted deployment pattern
       - "German clinical text" — explain: German medical NLP is underrepresented in open-source tooling; German-specific challenges (compound words like "Krankenhausaufenthalt", date formats DD.MM.YYYY, medical abbreviations)
       - "Plugin architecture" — explain: new entity types (e.g., allergies, procedures) can be added by creating one file implementing BaseExtractor without touching pipeline code
       - Keep to 3 short paragraphs; link to docs/ARCHITECTURE.md for full design details

    6. **## Architecture** — brief overview paragraph + link to docs/architecture.md:
       - One paragraph: "The pipeline uses a modular plugin architecture. POST /extract triggers parallel extraction via asyncio.gather across all registered extractors. Each extractor calls llama-cpp-python with a JSON schema constraint, returns validated Pydantic entities, and results are merged into EntityResponse."
       - Link: "See [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) for component diagrams, plugin pattern details, and how to add a new entity type."

    7. **## Development** — two subsections:
       - ### Running Tests: `docker-compose run api pytest` or `pip install ".[dev]" && pytest`
       - ### Adding a New Entity Type: 3-step summary — (1) create src/extraction/my_extractor.py implementing BaseExtractor, (2) define Pydantic schema in same file, (3) use @register_extractor decorator; full walkthrough in docs/ARCHITECTURE.md

    8. **## License** — MIT license, link to LICENSE file if present; author credit to Nargiz Mammadli

    Style rules (per D-07, documentation style from docs/ARCHITECTURE.md): H2 for major sections, H3 for subsections, fenced code blocks with language tags (bash, python, json), reference design decisions in comments where relevant.
  </action>
  <verify>
    <automated>
      grep "## Quick Start" README.md
      grep "Content-Type: application/json" README.md
      grep "import httpx" README.md
      grep "docs/ARCHITECTURE.md" README.md
      grep "docker-compose up" README.md
      grep "temporal_entities" README.md
      grep "## Why This Project" README.md
    </automated>
  </verify>
  <done>
    - README.md contains all 8 sections: header, Quick Start, API Reference, Example Extraction Output, Why This Project, Architecture, Development, License
    - Every curl example includes -H "Content-Type: application/json"
    - Python httpx example is present and imports httpx
    - docs/ARCHITECTURE.md is linked from the Architecture section
    - Quick Start section uses docker-compose up and includes the wget model download command
  </done>
</task>

<task type="auto">
  <name>Task 2: Add request-flow component diagram to docs/ARCHITECTURE.md</name>
  <files>docs/ARCHITECTURE.md</files>
  <read_first>
    - docs/ARCHITECTURE.md — read the full file to understand current structure; identify the exact location to insert the new "Request Flow" section (before the Plugin Pattern section per PATTERNS.md); confirm which D-09 sections are already present (plugin pattern and "how to add entity type" are confirmed present; component diagram is missing)
    - src/api/extract.py — read to understand actual POST /extract handler signature and how it calls extractors
    - src/extraction/base.py — read extractor registry and asyncio.gather pattern used in pipeline
    - .planning/phases/03-deployment-documentation/03-CONTEXT.md — D-09 section 1: "Component diagram showing main.py → pipeline → extractors → validators data flow"
    - .planning/phases/03-deployment-documentation/03-PATTERNS.md — docs/ARCHITECTURE.md section: what is missing (component diagram + asyncio.gather parallel sequence); documentation style (H2/H3, code blocks with language tags)
  </read_first>
  <action>
    Insert a new "## Request Flow" section at the top of docs/ARCHITECTURE.md, immediately before the existing "## Plugin Pattern" section (do not rewrite or remove any existing content).

    The section must contain:

    1. A brief introduction paragraph: "When a POST /extract request arrives, the pipeline runs all registered extractors in parallel using asyncio.gather, merges results, and returns a validated EntityResponse."

    2. An ASCII component diagram showing the full request path:
       ```
       POST /extract
            |
            v
       src/api/extract.py
       (ExtractRequest validated by Pydantic)
            |
            v
       asyncio.gather([                    <- parallel execution per D-17
           TemporalExtractor.extract(text),
           ClinicalExtractor.extract(text)
       ])
            |
            v
       Merge results (list.extend)        <- per D-18
       Confidence filtering               <- per D-20, CONFIDENCE_THRESHOLD env var
            |
            v
       EntityResponse (Pydantic validation)
       {
         temporal_entities: [...],
         clinical_entities: [...],
         errors: [...],
         low_confidence: [...]
       }
            |
            v
       JSON response to client
       ```

    3. A subsection "### Parallel Extraction" explaining:
       - asyncio.to_thread wraps synchronous llama-cpp-python calls (per D-17) so they don't block the event loop
       - return_exceptions=True in asyncio.gather preserves partial results if one extractor fails (per D-19)
       - Entities from all extractors are merged with list.extend() (per D-18)

    The inserted section must use the same documentation style as the rest of the file: H2/H3 headers, code blocks with language tags (ascii for diagrams), inline references to source files and decision IDs.
  </action>
  <verify>
    <automated>
      grep "## Request Flow" docs/ARCHITECTURE.md
      grep "asyncio.gather" docs/ARCHITECTURE.md
      grep "POST /extract" docs/ARCHITECTURE.md
      grep "EntityResponse" docs/ARCHITECTURE.md
    </automated>
  </verify>
  <done>
    - docs/ARCHITECTURE.md contains a new "## Request Flow" section with ASCII component diagram
    - Diagram shows: POST /extract → extract.py → asyncio.gather → extractors → EntityResponse → JSON
    - Section references D-17, D-18, D-19, D-20 for traceability
    - All existing content in docs/ARCHITECTURE.md is preserved (no deletions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → reader | Documentation is public-facing; must not expose internal paths, secrets, or security weaknesses |
| curl examples → POST /extract | Examples must include correct Content-Type header to avoid 422 errors for users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | README.md — model download URL | accept | URL points to public HuggingFace model (TheBloke/mistral-community); no private data; URL is public by design |
| T-03-07 | Tampering | README curl examples with wrong Content-Type | mitigate | Every curl example in README.md includes -H "Content-Type: application/json" (per RESEARCH.md Pitfall 5 prevention) |
| T-03-08 | Information Disclosure | docs/ARCHITECTURE.md internal paths | accept | All referenced paths are open-source code paths; no secrets, credentials, or internal infrastructure exposed |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — documentation only; no install tasks |
</threat_model>

<verification>
After both tasks complete:

1. README.md has 8 sections including Quick Start, API Reference (with curl + httpx), Example Extraction Output, Why This Project, Architecture (with docs/ARCHITECTURE.md link), Development, License
2. Every curl example in README.md includes -H "Content-Type: application/json"
3. docs/ARCHITECTURE.md has a new "## Request Flow" section with ASCII component diagram before the Plugin Pattern section
4. docs/ARCHITECTURE.md retains all existing content (plugin pattern, how to add entity type walkthrough)
</verification>

<success_criteria>
- Portfolio reviewer can read Quick Start and reach a working curl test without reading anything else
- curl and Python httpx examples are correct and complete (D-06)
- README explains design rationale in explanatory tone (D-07)
- docs/ARCHITECTURE.md covers all three D-09 sections: component diagram, plugin architecture, how to add entity type
- README links to docs/ARCHITECTURE.md (D-08)
</success_criteria>

## Artifacts this phase produces

**Modified files:**
- `README.md` — full portfolio document (8 sections): Quick Start, API Reference, Example Extraction Output, Why This Project, Architecture, Development, License
- `docs/ARCHITECTURE.md` — new "## Request Flow" section added at top (before Plugin Pattern), containing ASCII component diagram and Parallel Extraction subsection

**No new symbols (documentation only).**

<output>
Create `.planning/phases/03-deployment-documentation/03-02-SUMMARY.md` when done.
</output>
