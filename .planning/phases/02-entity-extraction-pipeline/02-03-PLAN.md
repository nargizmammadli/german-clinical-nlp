---
phase: 02-entity-extraction-pipeline
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/validation/validators.py
  - src/validation/__init__.py
  - src/api/extract.py
  - src/config.py
  - .env.example
  - tests/test_validation.py
autonomous: true
requirements: [VAL-02, API-04, ENTITY-05, ENTITY-06]

must_haves:
  truths:
    - System rejects future dates (impossible for clinical records)
    - System rejects confidence scores outside 0.0-1.0 range (handled by Pydantic)
    - Entities below confidence threshold moved to low_confidence array per D-06
    - API returns meaningful error messages for validation failures per D-08
    - Empty text returns 400 Bad Request with clear error message
  artifacts:
    - path: src/validation/validators.py
      provides: Domain validators for impossible values
      exports: [validate_date_not_future, validate_confidence_threshold, filter_low_confidence]
      min_lines: 50
    - path: tests/test_validation.py
      provides: Validation logic tests
      min_lines: 80
  key_links:
    - from: src/api/extract.py
      to: src/validation/validators.py
      via: validation function calls
      pattern: "validate_date_not_future|filter_low_confidence"
---

<objective>
Production-ready validation that rejects impossible values and provides detailed error feedback.

**Purpose:** Complete VAL-02 (domain validators) and API-04 (meaningful errors). Implements confidence threshold filtering per D-06 and detailed error responses per D-08.

**Output:** Extraction endpoint validates dates, filters low-confidence entities, and returns structured error messages for all failure modes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-entity-extraction-pipeline/02-CONTEXT.md
@.planning/phases/02-entity-extraction-pipeline/02-02-PLAN.md

# Existing schemas and extraction logic
@src/schemas/entities.py
@src/api/extract.py
@src/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain Validation Functions</name>
  <files>src/validation/validators.py, src/validation/__init__.py, tests/test_validation.py</files>
  
  <read_first>
    - .planning/phases/02-entity-extraction-pipeline/02-CONTEXT.md (D-06: confidence threshold, D-07: minimal validators, D-08: detailed errors)
    - src/schemas/entities.py (TemporalEntity and ClinicalEntity structure)
    - data/samples/ggponc_samples.json (date format examples - DD.MM.YYYY)
  </read_first>
  
  <behavior>
    - validate_date_not_future rejects dates after today, returns error message with the invalid date
    - filter_low_confidence moves entities below threshold to separate array
    - Both functions preserve valid entities and provide specific error details
  </behavior>
  
  <action>
    Create tests/test_validation.py with TDD structure (RED phase first):
    
    **test_validate_date_not_future_accepts_past:** Call validate_date_not_future with TemporalEntity(type="Date", text="15.03.2023", ...). Assert returns (True, None).
    
    **test_validate_date_not_future_rejects_future:** Call validate_date_not_future with future date "15.03.2027". Assert returns (False, "Date is in the future: 15.03.2027").
    
    **test_filter_low_confidence_threshold_50:** Create 4 entities with confidence [0.3, 0.5, 0.7, 0.9]. Call filter_low_confidence(entities, threshold=0.5). Assert high_conf has 3 items (>= 0.5), low_conf has 1 item (< 0.5).
    
    **test_filter_low_confidence_empty_input:** Call filter_low_confidence([], threshold=0.5). Assert returns ([], []).
    
    Run pytest tests/test_validation.py - tests MUST fail (RED).
    
    Create src/validation/validators.py implementing per D-06, D-07:
    
    **validate_date_not_future(entity: TemporalEntity) -> tuple[bool, str | None]:**
    - If entity.type != "Date", return (True, None) (only validate dates, not LOS)
    - Parse entity.text as DD.MM.YYYY format using datetime.strptime
    - Compare to datetime.now()
    - If future: return (False, f"Date is in the future: {entity.text}")
    - If past or today: return (True, None)
    - If parse fails: return (False, f"Invalid date format: {entity.text}")
    
    **filter_low_confidence(entities: list, threshold: float) -> tuple[list, list]:**
    - Per D-06: split entities by confidence >= threshold
    - Return (high_confidence_entities, low_confidence_entities)
    - Preserve original entity objects unchanged
    
    Create empty src/validation/__init__.py.
  </action>
  
  <verify>
    <automated>pytest tests/test_validation.py -v 2>&1 | grep -E "passed|PASSED" | wc -l | grep -v '^0$'</automated>
  </verify>
  
  <acceptance_criteria>
    - tests/test_validation.py has 4+ test functions covering date validation and confidence filtering
    - Tests initially fail (RED phase confirmed)
    - src/validation/validators.py contains validate_date_not_future and filter_low_confidence functions
    - validate_date_not_future parses DD.MM.YYYY format matching ggponc_samples.json
    - validate_date_not_future returns tuple of (bool, str | None) for valid/error
    - filter_low_confidence returns tuple of (high_conf_list, low_conf_list)
    - All validation tests pass after implementation (GREEN phase)
  </acceptance_criteria>
  
  <done>Domain validators reject future dates and filter low-confidence entities with clear error messages</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confidence Threshold Configuration</name>
  <files>src/config.py, .env.example</files>
  
  <read_first>
    - src/config.py (existing environment variable pattern)
    - .env.example (existing configuration template)
    - .planning/phases/02-entity-extraction-pipeline/02-CONTEXT.md (D-06: CONFIDENCE_THRESHOLD environment variable)
  </read_first>
  
  <behavior>
    - CONFIDENCE_THRESHOLD environment variable controls filtering
    - Default value is 0.5 if not set
    - Value must be between 0.0 and 1.0 (validated at startup per fail-fast pattern)
  </behavior>
  
  <action>
    Update src/config.py following existing MODEL_PATH pattern:
    
    1. After MODEL_NAME extraction, add: CONFIDENCE_THRESHOLD_RAW = os.getenv("CONFIDENCE_THRESHOLD", "0.5")
    
    2. Convert and validate: CONFIDENCE_THRESHOLD = float(CONFIDENCE_THRESHOLD_RAW); if not 0.0 <= CONFIDENCE_THRESHOLD <= 1.0: raise ValueError(f"CONFIDENCE_THRESHOLD must be between 0.0 and 1.0, got {CONFIDENCE_THRESHOLD}")
    
    3. Export CONFIDENCE_THRESHOLD at module level
    
    Update .env.example:
    
    Add after MODEL_NAME:
    ```
    # Confidence threshold for entity filtering (0.0-1.0, default: 0.5)
    # Entities below this threshold are moved to low_confidence array
    CONFIDENCE_THRESHOLD=0.5
    ```
  </action>
  
  <verify>
    <automated>python -c "from src import config; assert hasattr(config, 'CONFIDENCE_THRESHOLD'); assert 0.0 <= config.CONFIDENCE_THRESHOLD <= 1.0; print(f'Threshold: {config.CONFIDENCE_THRESHOLD}')"</automated>
  </verify>
  
  <acceptance_criteria>
    - src/config.py extracts CONFIDENCE_THRESHOLD from environment with default "0.5"
    - src/config.py validates threshold is between 0.0 and 1.0, raises ValueError if outside range
    - src/config.py exports CONFIDENCE_THRESHOLD as float
    - .env.example includes CONFIDENCE_THRESHOLD with explanation comment
    - Config validation happens at module import (fail-fast per D-09)
  </acceptance_criteria>
  
  <done>Confidence threshold configurable via environment variable with validation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate Validation into Extraction Endpoint</name>
  <files>src/api/extract.py, tests/test_extraction.py</files>
  
  <read_first>
    - src/api/extract.py (current extraction logic)
    - src/validation/validators.py (validation functions to integrate)
    - src/config.py (CONFIDENCE_THRESHOLD to import)
    - .planning/phases/02-entity-extraction-pipeline/02-CONTEXT.md (D-05: partial results, D-08: detailed errors)
  </read_first>
  
  <behavior>
    - After extraction, validate all date entities are not in future
    - Future dates moved to errors array with specific message per D-08
    - After validation, filter entities by confidence threshold per D-06
    - Low-confidence entities moved to low_confidence array in response
    - Valid high-confidence entities in temporal_entities and clinical_entities arrays
    - Partial results returned even if some entities fail validation per D-05
  </behavior>
  
  <action>
    Update src/api/extract.py extract_entities function:
    
    1. Import at top: from src.validation.validators import validate_date_not_future, filter_low_confidence; from src import config
    
    2. After asyncio.gather merges results, add validation loop:
    ```python
    validated_temporal = []
    validation_errors = []
    for entity in temporal_result["temporal_entities"]:
        is_valid, error_msg = validate_date_not_future(entity)
        if is_valid:
            validated_temporal.append(entity)
        else:
            validation_errors.append(error_msg)
    ```
    
    3. Apply confidence filtering per D-06:
    ```python
    high_conf_temporal, low_conf_temporal = filter_low_confidence(validated_temporal, config.CONFIDENCE_THRESHOLD)
    high_conf_clinical, low_conf_clinical = filter_low_confidence(clinical_result["clinical_entities"], config.CONFIDENCE_THRESHOLD)
    low_confidence_all = low_conf_temporal + low_conf_clinical
    ```
    
    4. Return EntityResponse with separated arrays: EntityResponse(temporal_entities=high_conf_temporal, clinical_entities=high_conf_clinical, errors=validation_errors, low_confidence=low_confidence_all)
    
    Update tests/test_extraction.py:
    
    Add **test_extract_rejects_future_dates:** POST /extract with text "Termin am 15.03.2027" (future). Assert response.errors contains "Date is in the future", temporal_entities is empty or excludes that date.
    
    Add **test_extract_filters_low_confidence:** Mock extractor to return entities with confidence [0.3, 0.7]. Assert high-confidence (0.7) in temporal_entities, low-confidence (0.3) in low_confidence array.
  </action>
  
  <verify>
    <automated>pytest tests/test_extraction.py -v 2>&1 | grep -E "test_extract_rejects_future_dates.*PASSED|test_extract_filters_low_confidence.*PASSED" | wc -l | grep '^2$'</automated>
  </verify>
  
  <acceptance_criteria>
    - src/api/extract.py imports validate_date_not_future, filter_low_confidence, config.CONFIDENCE_THRESHOLD
    - Extraction endpoint validates all temporal entities with validate_date_not_future
    - Validation errors added to errors array in response per D-08
    - Confidence filtering applied to both temporal and clinical entities
    - EntityResponse.low_confidence populated with entities below threshold per D-06
    - EntityResponse.errors populated with specific validation messages
    - Valid high-confidence entities in main temporal_entities and clinical_entities arrays
    - tests/test_extraction.py has test_extract_rejects_future_dates and test_extract_filters_low_confidence passing
  </acceptance_criteria>
  
  <done>Extraction endpoint validates dates, filters confidence, and returns detailed errors with partial results</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → validation | Same as previous plans |
| Validated entities → response | Filtered entities cross into user-visible output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | Validation errors | mitigate | Per D-08: error messages contain only entity text and validation failure reason, no internal state or model details |
| T-02-08 | Repudiation | Low confidence filtering | accept | Low-confidence entities returned in separate array for transparency - user sees all LLM output, decision boundary is explicit |
</threat_model>

<verification>
**Automated:**
```bash
# All validation tests pass
pytest tests/test_validation.py -v

# Extraction tests pass with validation
pytest tests/test_extraction.py -v

# Config exports threshold
python -c "from src.config import CONFIDENCE_THRESHOLD; assert 0.0 <= CONFIDENCE_THRESHOLD <= 1.0; print(f'Threshold configured: {CONFIDENCE_THRESHOLD}')"

# Env example includes threshold
grep -q "CONFIDENCE_THRESHOLD" .env.example
```

**Manual (requires GGUF model):**
1. Set .env with CONFIDENCE_THRESHOLD=0.6
2. Start server: `uvicorn src.main:app --reload`
3. Test future date rejection: `curl -X POST http://localhost:8000/extract -H "Content-Type: application/json" -d '{"text": "Termin am 15.03.2027"}'` - verify errors array contains "Date is in the future"
4. Test confidence filtering: Extract text with low-confidence entities - verify low_confidence array is populated
5. Test valid extraction: `curl -X POST http://localhost:8000/extract -d '{"text": "Aufnahme am 15.03.2023. Diagnose: Lumbalgie."}'` - verify temporal_entities and clinical_entities populated, errors empty
6. Check OpenAPI docs show low_confidence and errors arrays in response schema
</verification>

<success_criteria>
1. Future dates rejected with specific error message per VAL-02
2. Confidence threshold filters entities into high_confidence and low_confidence arrays per D-06
3. CONFIDENCE_THRESHOLD configurable via environment variable with default 0.5
4. Validation errors returned in structured errors array with entity details per D-08, API-04
5. Partial results preserved - validation failure for one entity doesn't block others per D-05
6. All tests pass including new validation tests
7. Empty text input returns 400 with meaningful error (API-04 complete)
</success_criteria>

<output>
Create `.planning/phases/02-entity-extraction-pipeline/02-03-SUMMARY.md` when done.

Document:
- Validation accuracy on sample data
- Confidence threshold impact on result quality
- Error message clarity for portfolio reviewers
</output>

## Artifacts This Phase Produces

**New symbols (exclude from drift verification):**

**Functions:**
- `validate_date_not_future` (src/validation/validators.py)
- `filter_low_confidence` (src/validation/validators.py)

**Module-level:**
- `CONFIDENCE_THRESHOLD` (src/config.py) - float configuration value

**New files:**
- src/validation/validators.py
- src/validation/__init__.py
- tests/test_validation.py

**Modified behavior:**
- `extract_entities` (src/api/extract.py) - now includes validation and confidence filtering
